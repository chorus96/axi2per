package:
  name: axi2per

dependencies:
  axi_slice: { git: "https://github.com/chorus96/axi_slice.git", version: 1.1.4 }

sources:
  # Level 0
  - src/axi2per_req_channel.sv
  - src/axi2per_res_channel.sv
  # Level 1
  - src/axi2per.sv

  # Simulation-only sources (tb target)
  - target: simulation
    files:
      - sim/per_slave_model.sv
      - sim/tb_axi2per.sv
